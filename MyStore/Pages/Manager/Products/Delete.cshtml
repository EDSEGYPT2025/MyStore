@page "{id:int}"
@model MyStore.Pages.Manager.Products.DeleteModel
@{
    ViewData["Title"] = "حذف المنتج";
    Layout = "~/Pages/Shared/_ManagerLayout.cshtml";
}

<div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 gap-4">
    <div>
        <h1 class="text-2xl font-bold text-destructive">@ViewData["Title"]</h1>
        <p class="text-muted-foreground mt-1">
            يرجى مراجعة التفاصيل أدناه قبل تأكيد عملية الحذف.
        </p>
    </div>
</div>

<div class="max-w-3xl mx-auto space-y-6">
    <div class="bg-destructive/10 border-l-4 border-destructive text-destructive p-4 rounded-md" role="alert">
        <div class="flex">
            <div class="py-1">
                <i data-lucide="alert-triangle" class="w-6 h-6 me-3"></i>
            </div>
            <div>
                <p class="font-bold">تحذير! أنت على وشك حذف منتج نهائياً.</p>
                <p class="text-sm">هذا الإجراء لا يمكن التراجع عنه. سيتم حذف جميع البيانات المتعلقة بهذا المنتج.</p>
            </div>
        </div>
    </div>

    <div class="bg-card text-card-foreground rounded-xl border shadow-sm">
        <div class="p-6">
            <h3 class="text-lg font-semibold mb-4">تفاصيل المنتج الذي سيتم حذفه:</h3>
            <div class="space-y-3 text-sm">
                <div class="flex justify-between">
                    <span class="text-muted-foreground">اسم المنتج:</span>
                    <span class="font-medium text-foreground">@Model.Product.Name</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-muted-foreground">الماركة:</span>
                    <span class="font-medium text-foreground">@Model.Product.Company.Name</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-muted-foreground">السعر:</span>
                    <span class="font-semibold text-primary">@Model.Product.Price.ToString("N2") ج.م</span>
                </div>
            </div>
        </div>

        <div class="px-6 py-4 bg-muted/50 border-t flex justify-end items-center gap-3">
            <form method="post" class="flex items-center gap-3">
                <input type="hidden" asp-for="Product.Id" />
                <a asp-page="./Index" class="px-4 py-2 text-sm font-medium rounded-md hover:bg-muted">إلغاء</a>
                <button type="submit" class="inline-flex items-center px-4 py-2 bg-destructive text-destructive-foreground font-semibold rounded-md shadow-sm hover:bg-destructive/90">
                    <i data-lucide="trash-2" class="w-4 h-4 me-2"></i>
                    نعم، قم بالحذف الآن
                </button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        lucide.createIcons();
    </script>
}