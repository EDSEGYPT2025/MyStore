@page
@model MyStore.Pages.Manager.Products.IndexModel
@{
    ViewData["Title"] = "إدارة المنتجات";
    Layout = "~/Pages/Shared/_ManagerLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3">@ViewData["Title"]</h1>
    <a asp-page="Create" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>إضافة منتج جديد
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        @if (Model.ProductList.Any())
        {
            <table class="table table-hover table-striped mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width: 80px;">الصورة</th>
                        <th>اسم المنتج</th>
                        <th>الماركة</th>
                        <th>السعر</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ProductList)
                    {
                        <tr>
                            <td>
                                <img src="@(item.ImageUrl ?? "/images/placeholder.png")" class="img-fluid rounded" style="width: 60px; height: 60px; object-fit: cover;" />
                            </td>
                            <td class="align-middle">@item.Name</td>
                            <td class="align-middle">@item.Company.Name</td>
                            <td class="align-middle">@item.Price.ToString("C", new System.Globalization.CultureInfo("ar-EG"))</td>
                            <td class="text-end align-middle">
                                <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary">تعديل</a>
                                <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">تفاصيل</a>
                                <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">حذف</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="p-4 text-center text-muted">
                <i class="fas fa-box-open fa-3x mb-3"></i>
                <p>لم تقم بإضافة أي منتجات بعد. اضغط على "إضافة منتج جديد" للبدء.</p>
            </div>
        }
    </div>
</div>
